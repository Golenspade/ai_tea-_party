{
  "name": "ai-tea-party",
  "version": "2.1.0",
  "private": true,
  "description": "AI Tea Party monorepo with TypeScript backend migration",
  "license": "MIT",
  "workspaces": [
    "frontend",
    "backend",
    "packages/*"
  ],
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r run build",
    "lint": "pnpm -r run lint"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["better-sqlite3"]
  }
}
