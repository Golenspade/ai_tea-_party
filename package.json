{
  "name": "ai-tea-party",
  "version": "2.2.0",
  "private": true,
  "description": "AI Tea Party — TypeScript monorepo (Next.js + Fastify/Pi Agent)",
  "license": "MIT",
  "workspaces": [
    "frontend",
    "backend",
    "packages/*"
  ],
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r run build",
    "lint": "pnpm -r run lint",
    "test": "pnpm --filter ai-tea-party-backend test && pnpm --filter ai-tea-party-frontend test"
  },
  "packageManager": "pnpm@10.12.1",
  "pnpm": {
    "onlyBuiltDependencies": ["better-sqlite3"]
  }
}
