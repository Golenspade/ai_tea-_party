# TS backend migration notes (Phase 1 scaffold)

- This backend is a TypeScript skeleton that mirrors Python FastAPI REST/WS/SSE contracts.
- State is in-memory; persistence and Pi Agent runtime are intentionally postponed to later phases.
- Endpoints implemented here should be kept field-compatible with existing frontend calls.
